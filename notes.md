Need to do:

- Decide if you're happy with the videos that are currently being used for the background content.
- Make a glassmorphism style interface using CSS blur and animations on resizing.
- Decide what info from the API to display and create functions to display it.

- Known bugs: COlor change on chrome autofill.
